const te = {
    eye: ['glasses', 'hat'],
    face: ['lips'],
    foot: ['slipper', 'running'],
};

Object.entries(te).map(([key, value]) => {
    value.map((item) => item);
});

const clothes = [
    ['yellow_hat', 'headgear'],
    ['blue_sunglasses', 'eyewear'],
    ['green_turban', 'headgear'],
];
